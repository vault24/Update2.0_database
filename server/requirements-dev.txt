# ============================================================
# DEVELOPMENT-ONLY dependencies.
# Install locally with:  pip install -r requirements-dev.txt
# These are NOT installed in production (keeps the server lean and avoids
# accidentally enabling debug tooling).
# ============================================================
-r requirements.txt

# Debugging
django-debug-toolbar==4.2.0

# Linting / formatting
black==23.12.0
flake8==6.1.0

# Testing
pytest==7.4.3
pytest-django==4.7.0
hypothesis==6.92.1
