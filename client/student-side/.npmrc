# Make `npm install` resilient across the different npm versions found on
# servers. legacy-peer-deps prevents hard ERESOLVE failures on strict npm (v7+)
# when transitive peer ranges disagree; the app is verified to build with these
# versions regardless.
legacy-peer-deps=true
engine-strict=false
fund=false
audit=false
